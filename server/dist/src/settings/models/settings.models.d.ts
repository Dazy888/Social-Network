export interface ActivationProps {
    email: string;
    id: string;
}
export interface ChangePassProps {
    currentPass: string;
    newPass: string;
    id: string;
}
export interface SetProfileSettingsProps {
    name: string;
    location: string;
    id: string;
}
